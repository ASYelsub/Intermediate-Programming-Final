using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class MapBehavior : MonoBehaviour
{
    //This script goes on the Map object, which is found through Canvas>Active Element Image>Map
    //It controls how the player sees and interacts with the map... they can draw on it and it will save the image.
    //It shows the landmark in the middle of the island.
    void Start()
    {
        
    }

    void Update()
    {
        
    }
}
